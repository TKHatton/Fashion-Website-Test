import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'AI Fashion Awards - She Is AI',
  description: 'Celebrate AI-generated fashion design. Submit your AI-created fashion collections to the AI Fashion Awards.',
  keywords: 'AI fashion, AI design, fashion awards, AI-generated fashion, She Is AI',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
